class LoginForm{

  // Initialize email
  late String email;

  // Initialize password
  late String password;

  // Initialize device
  late String deviceId;

  // Initilize constructor
  LoginForm({this.email = "",this.password = "",this.deviceId = ""});
}